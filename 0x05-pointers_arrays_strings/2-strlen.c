#include  "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of the string
 * @s: the string
 *
 * Return: the length of the s string
 */
int _strlen(char *S)
{
	int i = 0;

	while (s[i])
		i++;
	return (i);
}

